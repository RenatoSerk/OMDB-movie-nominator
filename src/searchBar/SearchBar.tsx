import React, {Component, ReactElement} from 'react';

interface searchProps {}

export default class Dropzone extends Component<searchProps>{

    render() : ReactElement {
        return (<div>Inner city pressure</div>);
    }
}
